# vsacictl
This package handles vsacictl command line tool. This is an image that will contain all the dependencies required for vsa-control plane cicd.

# vsacictl jira <command>
Handles commands for jira tasks
