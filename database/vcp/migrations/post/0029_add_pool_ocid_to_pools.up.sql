-- Intentionally empty.
-- The pool_ocid column is managed by GORM AutoMigrate (see PoolOCID field in
-- core/datamodel/models.go), so the schema change does not need to be applied
-- via a SQL migration. This file is retained only to preserve the migration
-- version sequence in schema_migrations_post.
SELECT 1;
