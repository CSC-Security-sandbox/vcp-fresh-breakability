/**
 * sanitizeForPrompt — strips prompt injection patterns from text
 * before including it in LLM prompts (changelogs, metadata, etc.)
 */
export declare function sanitizeForPrompt(text: string): string;
//# sourceMappingURL=sanitize.d.ts.map