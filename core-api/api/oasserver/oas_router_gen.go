// Code generated by ogen, DO NOT EDIT.

package oasgenserver

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [3]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/v1beta/projects/"
			origElem := elem
			if l := len("/v1beta/projects/"); len(elem) >= l && elem[0:l] == "/v1beta/projects/" {
				elem = elem[l:]
			} else {
				break
			}

			// Param: "projectNumber"
			// Match until "/"
			idx := strings.IndexByte(elem, '/')
			if idx < 0 {
				idx = len(elem)
			}
			args[0] = elem[:idx]
			elem = elem[idx:]

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case '/': // Prefix: "/locations/"
				origElem := elem
				if l := len("/locations/"); len(elem) >= l && elem[0:l] == "/locations/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "locationId"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[1] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/"
					origElem := elem
					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'g': // Prefix: "getMultiplePools"
						origElem := elem
						if l := len("getMultiplePools"); len(elem) >= l && elem[0:l] == "getMultiplePools" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleV1betaGetMultiplePoolsRequest([2]string{
									args[0],
									args[1],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

						elem = origElem
					case 'p': // Prefix: "pools"
						origElem := elem
						if l := len("pools"); len(elem) >= l && elem[0:l] == "pools" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							switch r.Method {
							case "GET":
								s.handleV1betaListPoolsRequest([2]string{
									args[0],
									args[1],
								}, elemIsEscaped, w, r)
							case "POST":
								s.handleV1betaCreatePoolRequest([2]string{
									args[0],
									args[1],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET,POST")
							}

							return
						}
						switch elem[0] {
						case '/': // Prefix: "/"
							origElem := elem
							if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "poolId"
							// Leaf parameter
							args[2] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "DELETE":
									s.handleV1betaDeletePoolRequest([3]string{
										args[0],
										args[1],
										args[2],
									}, elemIsEscaped, w, r)
								case "GET":
									s.handleV1betaDescribePoolRequest([3]string{
										args[0],
										args[1],
										args[2],
									}, elemIsEscaped, w, r)
								case "PUT":
									s.handleV1betaUpdatePoolRequest([3]string{
										args[0],
										args[1],
										args[2],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "DELETE,GET,PUT")
								}

								return
							}

							elem = origElem
						}

						elem = origElem
					}

					elem = origElem
				}

				elem = origElem
			}

			elem = origElem
		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name        string
	summary     string
	operationID string
	pathPattern string
	count       int
	args        [3]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/v1beta/projects/"
			origElem := elem
			if l := len("/v1beta/projects/"); len(elem) >= l && elem[0:l] == "/v1beta/projects/" {
				elem = elem[l:]
			} else {
				break
			}

			// Param: "projectNumber"
			// Match until "/"
			idx := strings.IndexByte(elem, '/')
			if idx < 0 {
				idx = len(elem)
			}
			args[0] = elem[:idx]
			elem = elem[idx:]

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case '/': // Prefix: "/locations/"
				origElem := elem
				if l := len("/locations/"); len(elem) >= l && elem[0:l] == "/locations/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "locationId"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[1] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/"
					origElem := elem
					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'g': // Prefix: "getMultiplePools"
						origElem := elem
						if l := len("getMultiplePools"); len(elem) >= l && elem[0:l] == "getMultiplePools" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = V1betaGetMultiplePoolsOperation
								r.summary = "List specified pools"
								r.operationID = "v1beta_getMultiplePools"
								r.pathPattern = "/v1beta/projects/{projectNumber}/locations/{locationId}/getMultiplePools"
								r.args = args
								r.count = 2
								return r, true
							default:
								return
							}
						}

						elem = origElem
					case 'p': // Prefix: "pools"
						origElem := elem
						if l := len("pools"); len(elem) >= l && elem[0:l] == "pools" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							switch method {
							case "GET":
								r.name = V1betaListPoolsOperation
								r.summary = "List all pools"
								r.operationID = "v1beta_listPools"
								r.pathPattern = "/v1beta/projects/{projectNumber}/locations/{locationId}/pools"
								r.args = args
								r.count = 2
								return r, true
							case "POST":
								r.name = V1betaCreatePoolOperation
								r.summary = "Create a new pool"
								r.operationID = "v1beta_createPool"
								r.pathPattern = "/v1beta/projects/{projectNumber}/locations/{locationId}/pools"
								r.args = args
								r.count = 2
								return r, true
							default:
								return
							}
						}
						switch elem[0] {
						case '/': // Prefix: "/"
							origElem := elem
							if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "poolId"
							// Leaf parameter
							args[2] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "DELETE":
									r.name = V1betaDeletePoolOperation
									r.summary = "Delete a storage pool"
									r.operationID = "v1beta_deletePool"
									r.pathPattern = "/v1beta/projects/{projectNumber}/locations/{locationId}/pools/{poolId}"
									r.args = args
									r.count = 3
									return r, true
								case "GET":
									r.name = V1betaDescribePoolOperation
									r.summary = "Describe a pool"
									r.operationID = "v1beta_describePool"
									r.pathPattern = "/v1beta/projects/{projectNumber}/locations/{locationId}/pools/{poolId}"
									r.args = args
									r.count = 3
									return r, true
								case "PUT":
									r.name = V1betaUpdatePoolOperation
									r.summary = "Update a pool"
									r.operationID = "v1beta_updatePool"
									r.pathPattern = "/v1beta/projects/{projectNumber}/locations/{locationId}/pools/{poolId}"
									r.args = args
									r.count = 3
									return r, true
								default:
									return
								}
							}

							elem = origElem
						}

						elem = origElem
					}

					elem = origElem
				}

				elem = origElem
			}

			elem = origElem
		}
	}
	return r, false
}
