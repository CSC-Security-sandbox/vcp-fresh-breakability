# NOTE: version/appVersion in Chart.yaml is generated by pipelines; keep "three zeros" in code.
apiVersion: v2
name: vlm-worker
description: VLM worker Helm chart for OCI OKE (Oracle Kubernetes Engine)

type: application

version: "0.0.0"

appVersion: "v0.0.0"
