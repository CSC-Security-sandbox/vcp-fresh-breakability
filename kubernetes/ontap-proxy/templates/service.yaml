apiVersion: v1
kind: Service
metadata:
  name: ontap-proxy-service
  labels:
    app: {{ .Chart.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: {{ .Values.service.appPort }}
      name: http
  selector:
    app: {{ .Chart.Name }} 