apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: harvest-pvc
  name: harvest-pvc
  namespace: {{ $.Values.global.namespace }}
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ $.Values.persistentVolumes.storage }}
