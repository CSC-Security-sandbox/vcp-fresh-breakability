# Version in Chart.yaml file is generated by github pipelines on the fly. Always needs to be "three zeros" in code to work properly
apiVersion: v2
description: adc helm chart for VSA
name: adc
version: "0.0.0"
appVersion: "v0.0.0"
deprecated: false
annotations:
  gcp-component: "true"

